// Client Konfiguration Administrator Dashboard
const port = 3113;
const host = 'localhost';
const protocol = 'http';


module.exports = {
    PORT: port,
    HOST: protocol + '://' + host + ':' + port,
};